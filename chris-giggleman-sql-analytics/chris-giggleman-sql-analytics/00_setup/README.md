# Setup

Pick one engine (or use both). The projects are duplicated with engine-specific scripts.

- `sqlite/`: simplest local setup (DB Browser for SQLite + CSV import)
- `postgres/`: Docker-based setup with PostgreSQL

Recommended SQL client: **DBeaver** (works for both engines).
